# Answers — Checkpoint A

1. `npm i -g @anthropic-ai/claude-code`
2. No. Each `claude` session starts fresh by default. Memory is loaded from CLAUDE.md files (covered in Part III).
3. `/clear`
4. SQLite (via better-sqlite3)
5. 404 (with body `{"error": "no quips"}`)
