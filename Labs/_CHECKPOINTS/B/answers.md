# Answers — Checkpoint B

1. Specificity — add schema, function signature, or expected output.
2. Read — Edit requires a prior Read of the same file.
3. Executing edits or running non-read-only Bash commands.
4. `quips/.claude/settings.local.json` (or `.claude/settings.json`).
5. Update the tests' seed data or use `resetDb()`; do NOT loosen assertions.
