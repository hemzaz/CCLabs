# Answers — Checkpoint C

1. `quips/CLAUDE.md` (project memory at the working-dir root).
2. It forces Claude to Read that path into context immediately, rather than guess.
3. CLI flag > enterprise > local > project > user.
4. Partially — compaction summarizes and drops raw turns; earlier specifics may be lost, but the summary preserves key facts.
5. When Claude is working in the directory containing the nested file (or below).
