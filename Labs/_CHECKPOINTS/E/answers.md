# Answers — Checkpoint E

1. name, description, tools, model.
2. Auto-routing by description match OR explicit Task tool invocation.
3. Exit code 2 (or higher) blocks the tool call; exit 0 or 1 does not.
4. `.claude/skills/<skill-name>/SKILL.md` inside the project root.
5. `mcpServers` (an object mapping server name to command and args).
